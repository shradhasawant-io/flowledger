spring:
  datasource:
    url: jdbc:mysql://localhost:3306/flowledger
    username: root
    password: Root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

logging:
  level:
    org.hibernate.SQL: debug